apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: backend-role
  namespace: error-archive
  labels:
    app: backend
    security: enabled
rules:
# 최소 권한: Pod 상태 확인만 허용
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list"]

